The Lua Editor only lists the Welcome starter by default.

Keep real work in a local folder opened from Lua Editor. The example scripts in
this folder can be copied into that workspace when you want to test them.
